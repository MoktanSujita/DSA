{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile C program",
            "type": "shell",
            "command": "gcc",
            "args": [
                "${file}",
                "-o",
                "${fileDirname}/stac.exe"  // Output file will be in the same folder as your source file
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Generated task for compiling C program"
        }
    ]
}
